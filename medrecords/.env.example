# Backend environment variables (Spring Boot)
# Copy this to a .env file or set directly in Railway dashboard

MONGODB_URI=mongodb://localhost:27017/medrecords
JWT_SECRET=replace-this-with-a-long-random-secret-string
PORT=8080

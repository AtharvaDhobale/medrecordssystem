# Frontend environment variables (React)
# Copy this to a .env file in medrecords-frontend/

# Point to your backend URL (Railway URL in production, localhost for local dev)
REACT_APP_API_URL=http://localhost:8080
